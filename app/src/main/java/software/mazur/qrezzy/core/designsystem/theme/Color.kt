package software.mazur.qrezzy.core.designsystem.theme

import androidx.compose.ui.graphics.Color

val Mint = Color(0xFF25AD67)
val Yellow = Color(0xFFFFDC61)
val Pink = Color(0xFFF54081)
val Purple = Color(0xFF7A3DE0)

// Brand colors used across the QREZZY visual identity.
val QrezzyMint = Color(0xFFB8F2D7)
val QrezzyYellow = Color(0xFFFFE66D)
val QrezzyPink = Color(0xFFFFA1C2)
val QrezzyPurple = Color(0xFFC9A4F4)

// Main app background and reusable surfaces.
val Background = Color(0xFFF9F9F9)
val Surface = Color(0xFFFFFFFF)

// Text colors used for titles, descriptions and disabled states.
val TextPrimary = Color(0xFF222222)
val TextSecondary = Color(0xFF6B6B6B)
val TextDisabled = Color(0xFFA0A0A0)

// Borders used for cards, inputs, tabs and outlined components.
val BorderLight = Color(0xFFE5E5E5)
val BorderMedium = Color(0xFFD0D0D0)

// Status colors used for success, warning and error states.
val Success = Color(0xFF4CAF50)
val Warning = Color(0xFFFFB300)
val Error = Color(0xFFFF5A5A)

// Scanner screen colors.
// Used for scan frame, scan line, active scanner tab and scanner icons.
val ScannerGreen = Color(0xFF8EE6C3)
val ScannerGreenDark = Color(0xFF56C99C)

// QR generator screen colors.
// Used for generator icon, active generator tab and QR creation accents.
val GeneratorYellow = Color(0xFFFFE66D)
val GeneratorYellowDark = Color(0xFFF2D13E)

// History screen colors.
// Used for history icon, active history tab, favorite accents and saved item states.
val HistoryPink = Color(0xFFFFA1C2)
val HistoryPinkDark = Color(0xFFF57AA8)

// Settings screen colors.
// Used for settings icon, active settings tab and settings-related accents.
val SettingsPurple = Color(0xFFC9A4F4)
val SettingsPurpleDark = Color(0xFFA97BE8)
